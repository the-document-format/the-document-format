[[bin]]
name = "tdf-cli"
path = "src/main.rs"

[dependencies]
tdf-engine = {path = "../../crates/tdf-engine"}
clap = {version = "4", features = ["derive"]}
image = "0.25"
viuer = "0.8"

[package]
name = "tdf-cli"
version = "0.0.0"
edition = "2021"
